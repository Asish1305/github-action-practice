
name: Hello Workflow
on:
  push:
    branches:
      - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to users
        run: echo 'Hello dosto'
      - name: say hello to username
        run: echo 'hello $USER'
  Details:
    runs-on: ubuntu-latest
    steps:
      - name: detailed version of python and dpcker
        run: 'python -- version && docker -- version'
